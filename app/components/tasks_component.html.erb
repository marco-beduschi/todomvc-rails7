<section class="flex flex-col items-center">
  <div class="basis-44">
    <h1 class="font-light text-5xl text-pink-300 leading-loose">todos</h1>
  </div>
  <div class="bg-white h-fit p-2 min-w-[20rem] max-w-sm rounded shadow-md lg:shadow-xl">
    <%= render(NewTaskComponent.new(task: Task.new)) %>
    <ul class="flex flex-col">
      <%= render(TaskComponent.with_collection(@tasks)) %>
    </ul>
  </div>
</section>